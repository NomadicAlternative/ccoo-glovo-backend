DATABASE_URL="mysql://root:example@localhost:3306/ccoo_glovo"
PORT=3000

# JWT settings
# JWT_SECRET should be a strong random string in production
JWT_SECRET=change-me-in-production
# Access token lifetime (e.g. 15m, 7d) or 'never' for dev only
JWT_EXPIRES_IN=15m
# Refresh token lifetime in days (e.g. 30d)
JWT_REFRESH_EXPIRES_IN=30d
